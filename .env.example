# Database Configuration
DB_SERVER=localhost
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=YourStrongPassword123!
DB_NAME=InventoryDB

# Set to true if using Azure SQL or requiring encryption
DB_ENCRYPT=false
DB_TRUST_SERVER_CERTIFICATE=true

# For Docker Compose (uses same password)
# Run: docker compose up -d
