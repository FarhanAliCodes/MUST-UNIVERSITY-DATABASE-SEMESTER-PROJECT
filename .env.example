# REQUIRED: Copy this file to .env before running docker compose
# cp .env.example .env

# Database password (REQUIRED - must be strong: 8+ chars, uppercase, lowercase, number, symbol)
DB_PASSWORD=YourStrongPassword123!

# Database name (optional)
DB_NAME=InventoryDB
